export enum UserRoles {
    MANAGER = 'Manager',
    TEAM_LEADER = 'Team Leader',
    EMPLOYEE = 'Employee'
}

export enum TaskStatuses {
    COMPLETED = 'Completed',
    IN_PROGRESS = 'In Progress',
    PENDING = 'Pending'
}